import { FC } from 'react';
import AppBar from '../../Components/AppBar';

const InitialPage: FC = () => {
  return (
    <div>
      <h1>Págins inicial</h1>
      <AppBar />
    </div>
  );
};

export default InitialPage;
